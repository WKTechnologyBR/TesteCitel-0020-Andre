import 'package:flutter/material.dart';

class InfoSemDados extends StatelessWidget {
  
  const InfoSemDados({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Sem dados para mostrar'),);
  }
}